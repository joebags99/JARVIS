# Developer tooling for the test + lint gate. This intentionally does NOT pull
# in requirements.txt: the unit tests cover pure logic and only need a handful
# of light runtime deps, so CI stays fast and avoids the heavy/native packages
# (pywebview, faster-whisper, …) that the desktop app needs but the tests don't.

pytest>=8,<10
ruff>=0.6

# Light runtime deps imported directly by the unit tests.
python-dotenv>=1.0.0
parsedatetime>=2.6
requests>=2.31.0
# Pure image I/O only (resize/encode) — no display needed, so it's safe here
# unlike the native audio/whisper stack. Used by tests/test_screenshot.py.
Pillow>=10.2.0
# Pure-Python iCalendar parsing (no native deps) — both imported at module
# load by integrations/outlook_ics.py, which tests/test_calendar_sources.py
# exercises.
icalendar>=5.0.0
recurring-ical-events>=2.1.0
