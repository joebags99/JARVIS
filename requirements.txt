# ── Core ─────────────────────────────────────────────────────
# >=0.60.0 needed for the native web_search tool type used by meal prep.
# Upper bound: the SDK is still 0.x and can break across minor bumps; the app
# also relies on beta MCP/web-search shapes, so pin below the next major.
anthropic>=0.60.0,<1.0
python-dotenv>=1.0.0

# ── UI / tray ────────────────────────────────────────────────
# Upper bound: pywebview majors change the backend/transparency surface this
# app pokes at directly (layered windows, evaluate_js), so cap the major.
pywebview>=5.1,<7
pythonnet>=3.0.3; sys_platform == 'win32'
pystray>=0.19.5
Pillow>=10.2.0

# ── Voice input (optional — see README if install fails) ─────
sounddevice>=0.4.6
scipy>=1.11.0
# Upper bound: numpy majors (the 2.0 ABI break) ripple through scipy/whisper.
numpy>=1.24.0,<3
faster-whisper>=1.0.0

# ── Wake word (optional — "Hey JARVIS" hands-free activation) ─
# onnx (not tflite): matches the numpy>=1.24,<3 pin above without pulling in
# a second, less broadly-compatible inference runtime.
openwakeword>=0.6.0
onnxruntime>=1.17.0

# ── Text-to-speech (optional — only what your chosen TTS_ENGINE needs) ──
# edge  → edge-tts + miniaudio (free neural, online).  system → pyttsx3 (offline).
# elevenlabs needs no extra package (uses requests, already above).
edge-tts>=6.1.0
miniaudio>=1.59
pyttsx3>=2.90

# ── Notes ingestion ──────────────────────────────────────────
watchdog>=4.0.0

# ── Google Calendar (optional) ───────────────────────────────
google-api-python-client>=2.0.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# ── Timezone detection ─────────────────────────────────────────
tzlocal>=5.0
tzdata>=2024.1; sys_platform == 'win32'  # zoneinfo needs this on Windows for America/New_York lookups

# ── Outlook / Microsoft Graph (optional) ─────────────────────
msal>=1.28.0
requests>=2.31.0

# ── Outlook ICS feed (optional, no-Azure-access fallback) ────
icalendar>=5.0.0
recurring-ical-events>=2.1.0

# ── Todoist (optional) ────────────────────────────────────────
parsedatetime>=2.6

# ── Global hotkey (optional) ─────────────────────────────────
keyboard>=0.13.5


